#include "visuals.hpp"

void visuals::run( )
{
    if ( !g_entity_list->local )
        return;

    for ( auto& entity : g_entity_list->enemy )
    {
        if ( !entity )
            continue;

         
    }
}