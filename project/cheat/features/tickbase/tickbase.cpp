#include "tickbase.hpp"

void tickbase::run( )
{
	// if ( !clock_drift )
	//	return;

	// if ( !last_tick )
	//	return;

	//( ( void( __thiscall* )( void*, int ) )( g_signatures[ HASH( "55 8B EC 53 56 8B 75 ? 57 8B F9 89 77" ) ].as< std::uintptr_t >( ) ) )(
	//clock_drift,
	//                                                                                                                                      last_tick
	//                                                                                                                                      );
}
